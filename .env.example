# API key for the LLM provider configured in app/config/default.yaml.
# The app talks to any OpenAI-compatible endpoint via langchain-openai,
# so this variable holds the key for whichever provider base_url points to
# (Groq by default).
OPENAI_API_KEY=your_key_here
